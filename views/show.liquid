<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Weather Search</title>

        <!-- link in bootstrap -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

        <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Rock+Salt&family=Shadows+Into+Light&family=ZCOOL+QingKe+HuangYou&display=swap" rel="stylesheet">

        <!-- link in our css -->
    </head>
        <link rel="stylesheet" href="/styles.css" />
        <body>
        <nav class="navbar navbar-expand-md navbar-dark bg-dark">
            <div class="container-fluid">
                
                <a class="navbar-brand">Current Weather</a>
                <a href="/weather" class="nav-link text-light">New Search</a>
                
            </div>
        </nav>
        

<div class="container-md">
    <article>
        <h2 class="location">
           📍{{ data.name }}
        </h2>

        {% assign temp = data.main.temp %}
        {% if temp < 40 %}
        <h3 class="temp" style="color: blue;">{{ temp }}℉</h2>
        {% elsif temp > 90 %}
        <h2 class="temp" style="color: red;">{{ temp }}℉</h2>
        {% else %}
        <h2 class="temp">{{ temp }}℉</h2>
        {% endif %}

        <strong>description:</strong> <p>{{ data.weather[0].description }}</p>
        Temp min: <h4>{{ data.main.temp_min }}</h4>
        Temp max: <h4>{{ data.main.temp_max }}</h4>   

       
    </article>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    </body>
</html>